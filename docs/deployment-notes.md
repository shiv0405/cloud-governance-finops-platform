# Deployment Notes

This file captures lightweight rollout guidance for `infra-reporting-starter`.

1. Review Terraform variables and state backend settings.
2. Run `terraform fmt` and `terraform validate` before applying.
3. Document environment-specific IAM or networking assumptions.
4. Replace sample values with production-ready identifiers before deployment.